import WebPlayer from '../components/WebPlayer'
import example from '../examples/text'


`Text` is used to render text. Unlike on the web, text <i>must</i> be wrapped in a `<Text>` component.

<WebPlayer {...example} />
