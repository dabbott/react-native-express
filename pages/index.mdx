import WebPlayer from '../components/WebPlayer'

JavaScript is a versatile language for building websites, servers, mobile apps, more!

This guide is intended for developers who know at least one other programming language well. We won't cover general programming concepts (e.g. _what is a variable?_), but rather JavaScript-specific concepts (e.g. _how do variables work in JavaScript?_).

<div style={{width: '100%'}} />