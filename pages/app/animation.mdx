# Animation

Approaches:

- `Animated` - built-in, for animating styles
- `react-native-reanimated` - "worklets" that run in a second JS context on the UI thread
- `LayoutAnimation` - built-in automatic animations, like "magic move"
