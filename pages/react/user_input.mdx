import userInput from '../../examples/files/react/userInput.js'

# User Input

In React, most input components are stateless.

An input component has a `value` prop and an `onChange` prop, and together these things give us complete control over the input without ever having to access the native UI imperatively.

---

## Input field

Here's a text input field:

<Example code={userInput} />
