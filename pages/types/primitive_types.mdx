import EditorConsole from '../../components/EditorConsole'
import literals from '!raw-loader!../../examples/files/types/literals.ts'
import primitiveConstructors from '!raw-loader!../../examples/files/types/primitiveConstructors.ts'
import primitiveMethods from '!raw-loader!../../examples/files/types/primitiveMethods.ts'
import primitiveCasting from '!raw-loader!../../examples/files/types/primitiveCasting.ts'

# Primitive types

There are 6 primitive types:

- `undefined`
- `Boolean`
- `Number`
- `String`
- `BigInt`
- `Symbol`

> We'll look at `undefined` when we cover nullability. `BigInt` and `Symbol` are less common.

---

## Primitive values

We declare primitive types using... primitive values (i.e. literals).

<EditorConsole   
  title={"Literal values"}
  code={literals}
/>

---

## Constructor functions

It's also possible to instantiate the built-in types using their constructors — however, this is relatively uncommon and not idiomatic for common usage.

<EditorConsole 
  title={'Constructor functions'}
  code={primitiveConstructors}
/>

---

## Standard library methods

We use standard library methods for manipulating primitives. These are available as methods on each instance.

> JavaScript uses _autoboxing_ to make these methods available even on literal values.

<EditorConsole 
  title={'Standard library methods'}
  code={primitiveMethods}
/>

---

## Type casting

There are library methods for casting strings to numbers and vice versa:

<EditorConsole 
  title={'Type casting'}
  code={primitiveCasting}
/>