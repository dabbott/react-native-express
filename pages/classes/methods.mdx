import EditorConsole from '../../components/EditorConsole'
import methods from '../../examples/files/classes/methods.ts'

Methods can be configured in a variety of ways:

- Static methods use the `static` keyword

<EditorConsole   
  title={"Methods"}
  code={methods}
/>
