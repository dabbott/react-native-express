import EditorConsole from '../../components/EditorConsole'
import enums from '../../examples/files/type_declarations/enums.ts'
import stringEnums from '../../examples/files/type_declarations/stringEnums.ts'

<EditorConsole
  title={"Enum types"}
  code={enums}
/>

## Enums with initializers

Enums can be declared with initializers. This is useful for creating enums of strings, or with specific number values.

<EditorConsole
  title={"String enum types"}
  code={stringEnums}
/>
