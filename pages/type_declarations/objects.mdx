import EditorConsole from '../../components/EditorConsole'
import structs from '../../examples/files/type_declarations/structs.ts'
import dictionaries from '../../examples/files/type_declarations/dictionaries.ts'

JavaScript objects are extremely versatile, so there are several different ways we can define their types.

## Structs/records

<EditorConsole   
  title={"Structs / Records"}
  code={structs}
/>

## Dictionaries/maps

<EditorConsole   
  title={"Dictionaries / Maps"}
  code={dictionaries}
/>