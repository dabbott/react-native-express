---
title: ScrollView
---

import example from '../../examples/scrollview'

# ScrollView

`ScrollView`s are used for scrollable content. They're well suited for scrolling small quantities of content (<10 items). They can scroll horizontally or vertically. For large quantities of items, consider using a `FlatList` for better performance.

<Example code={example.code} />
