import view from '../../examples/files/core_components/view.js'

# View

`View`s are the most basic building block of React Native apps. The `View` is an abstraction layer on top of the target platform's native equivalent:

- For iOS - `UIView`
- For Android - `android.view`
- For Web - `<div>`

---

## Example

`View`s are primarily used for styling and layout of children elements. <Details>Let's look at a few of the styles we can apply to views. The example below contains two `View`s: the outer view which specifies the alignment of the content contained within, and the inner view which draws a blue square.</Details>

<Example code={view} />
