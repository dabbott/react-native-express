const myBoolean = true

// Types are inferred!
const myNumber = 42

// But you can also add type annotations
const myString: string = 'Hello, world!'

console.log(myBoolean)
console.log(myNumber)
console.log(myString)
